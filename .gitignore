# Node / build output
node_modules/
dist/
coverage/
*.tsbuildinfo

# Versioned upload zips from `npm run package`
deploy/

# Logs
*.log
npm-debug.log*

# Environment
.env
.env.*
!.env.example

# Claude Code configuration and project instructions. PURGED from git history on
# 2026-08-23: machine paths, usernames and working preferences, none of which is
# GPL corresponding source and none of which the public needs. Build
# instructions and the engine invariants live in README.md instead.
# NOTE: ignored files do NOT sync between the desktop and the laptop — the
# canonical copies are in Dropbox (online_open_rocket_reference/).
.claude/
CLAUDE.md

# Editor/OS noise
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Spike artifacts (large JARs are fetched, not committed)
spikes/**/*.jar
!spikes/**/gradle-wrapper.jar

# Gradle
.gradle/
spikes/**/build/
engine-java/build/
.playwright-mcp/

# Project notes, session records, testing batches and tester-supplied designs.
# PURGED from git history on 2026-08-23 and deliberately kept out of the repo:
# this repo is PUBLIC (the GPL corresponding-source offer points at it), and
# none of this is corresponding source — it is working material.
# The user guide is NOT here: it is a build input and lives at
# packages/app/user-guide.md.
# NOTE: ignored files do NOT sync between the desktop and the laptop. Keep the
# canonical copy in Dropbox (online_open_rocket_reference/).
docs/
