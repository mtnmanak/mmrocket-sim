{
  "version": "0.076",
  "released": "2026-08-29",
  "note": "NO NUMBERS CHANGE — this is the visual batch. Four surface levels replace the near-flat dark theme (panels lift, controls raise, inputs recess; light theme reshaped the same way; Daylight untouched). Chart lines get a dark-stepped palette validated against the real panel color — the thrust line was under 2:1 and effectively invisible — with a color-blind-validated slot order that changes some line colors in both themes (velocity is now orange; panels are titled, so no reading changes). Breakdown components past eight get dash patterns instead of silently reusing colors, so LEM-IV's two rail buttons stop being twins. The Design canvas sizes to the rocket instead of the window — no more window-tall empty sky, footer back on screen, faint drafting grid in the sky. Warnings speak one ranked voice (left bar + tint + i/⚠/⛔), destructive buttons go red-outline, the Daylight aft-view black box is fixed, and the 3D view's Reset/Side/Aft/CG-CP buttons move out from under the floating chip to the top-right."
}
