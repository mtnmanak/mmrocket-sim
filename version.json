{
  "version": "0.067",
  "released": "2026-08-25",
  "note": "The drag breakdown adds up now: per-component rows carry the same instance counts the total uses (a four-fin set showed one fin's drag), same-named parts no longer merge into one row, and stage/pod CD overrides get their own row. Kernel rebuild, differential clean at 271 comparison points."
}
